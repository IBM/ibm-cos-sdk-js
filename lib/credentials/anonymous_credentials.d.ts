import {Credentials} from '../credentials';
export class AnonymousCredentials extends Credentials {
    /**
     * Creates a new AnonymousCredentials class for unauthenticated requests.
     */
    constructor();
}